@model T034.ViewModel.SettingViewModel

@{
    ViewBag.Title = "Редактирование";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@using (Html.BeginForm("AddOrEdit", "Setting"))
{
    <div class="control-group warning">
        <label class="control-label">@Html.LabelFor(m => m.Name)</label>
        <div>
            @Html.TextBoxFor(m => m.Name, new Dictionary<string, object> { { "class", "input-xxlarge" } })
            @Html.HiddenFor(m => m.Id)
        </div>
        
        <label class="control-label">@Html.LabelFor(m => m.Code)</label>
        <div>
            @Html.TextBoxFor(m => m.Code, new Dictionary<string, object> { { "class", "input-xxlarge" } })
        </div>
        
        <label class="control-label">@Html.LabelFor(m => m.Value)</label>
        <div>
            @Html.TextBoxFor(m => m.Value, new Dictionary<string, object> { { "class", "input-xxlarge" } })
        </div>

    </div>
    <div class="form-actions">
        <button type="submit" class="btn btn-primary">Сохранить изменения</button>
    </div>
}
