@model T034.Models.UserModel

@Model.Name 

@if (Model.IsAutharization)
{
    @Html.ActionLink("Выйти", "Logout", "Auth")
}
else
{
    @Html.ActionLink("Войти при помощи Яндекса", "RedirectToYandex", "Auth")
}
@using (Html.BeginForm("RedirectToYandex", "Auth"))
{
    @Html.Hidden("token")
}